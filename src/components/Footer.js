import React from 'react'
import styled from 'styled-components'

const Footer = () => {
  return <BottomMenu>Footer</BottomMenu>
}

const BottomMenu = styled.footer`
  background: #ccc;
`

export default Footer
